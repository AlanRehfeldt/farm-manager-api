export const IDEMPOTENCY_KEY_HEADER = 'idempotency-key';

export const IDEMPOTENCY_CONFLICT_MESSAGE = 'IDEMPOTENCY_CONFLICT';

/** Placeholder status while the original request is still executing. */
export const IDEMPOTENCY_PROCESSING_STATUS = 0;

export const IDEMPOTENT_METADATA_KEY = 'idempotent';
